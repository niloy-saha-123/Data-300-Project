# Data Directory

`data/raw/` holds the original Kaggle JSON files and should be treated as read-only.

`data/processed/` holds parsed and engineered outputs generated by the pipeline, such as:

- `portfolio_clean.parquet`
- `profile_clean.parquet`
- `transcript_flat.parquet`
- `offer_response.parquet`
- `features.parquet`
